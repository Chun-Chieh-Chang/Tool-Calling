node_modules/
.temp/
.exports/
.omo/
.agnes/
.claude/
.DS_Store
*.log
dist/
# 守衛測試的備份檔（正常情況下寫在 os.tmpdir()，此規則為防止回歸時污染工作區）
*.guardtest.bak
# Auto-regenerated by saveRegistry() – always stale in git
docs/knowledge-graph.html
*.mp4
strix_runs/
sync.ffs_db

# Local-only IDE rule exports — migrate to AgnesCode separately
docs/antigravity-global-rules.md

# 代理工作區（與上方 .claude/ .agnes/ .omo/ 同性質，非專案原始碼）
.workbuddy-ai/
# 一次性回滾快照（批次作業前產生，內含重複的 registry 全量資料，約 7MB）
.backup-*/

# Option B 預計算語意向量（可由 `npm run embed:build` 再生，含大量 float，不宜 version control）
registry/embeddings/
# 寫入 tools.json 前的自動備份（每次執行都產生一份，屬還原點非原始碼）
registry/backups/
# Kiro IDE 工作目錄（spec 草稿、任務狀態，屬工具私有狀態，非專案原始碼）
.kiro/
